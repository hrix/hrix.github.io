## install

```
git clone git@github.com:hrix/hrix.github.io.git
cd hrix.github.io
nvm install v8.5.0
npm install -g hexo
npm install
npm install hexo-renderer-ejs
npm install hexo-renderer-marked
npm install hexo-renderer-stylus
```
